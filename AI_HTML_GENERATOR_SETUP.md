# AI HTML Generator สำหรับประกาศ

## ภาพรวม
ฟีเจอร์นี้ใช้ OpenAI API เพื่อสร้างไฟล์ HTML ที่สวยงามและทันสมัยจากข้อความประกาศที่ผู้ใช้กรอกเข้ามา

## การตั้งค่า

### 1. ติดตั้ง Dependencies
ไม่ต้องติดตั้ง package เพิ่มเติม เพราะใช้ `fetch` API ที่มีอยู่แล้วใน browser

### 2. ตั้งค่า OpenAI API Key

#### วิธีที่ 1: ใช้ Environment Variable (แนะนำ)
เพิ่ม API key ในไฟล์ `.env`:

```env
VITE_OPENAI_API_KEY=sk-your-openai-api-key-here
```

#### วิธีที่ 2: ใช้ Supabase Edge Function (ปลอดภัยกว่า)
สร้าง Edge Function เพื่อเรียก OpenAI API จากฝั่ง server:

```typescript
// supabase/functions/generate-announcement-html/index.ts
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'

serve(async (req) => {
  const { title, content, priority, type, start_date, end_date, youtube_embed_url } = await req.json()
  
  const response = await fetch('https://api.openai.com/v1/chat/completions', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${Deno.env.get('OPENAI_API_KEY')}`
    },
    body: JSON.stringify({
      model: 'gpt-4o-mini',
      messages: [
        {
          role: 'system',
          content: 'คุณเป็นผู้เชี่ยวชาญด้านการออกแบบ HTML/CSS'
        },
        {
          role: 'user',
          content: `สร้าง HTML สวยงามสำหรับประกาศ: ${title}...`
        }
      ]
    })
  })
  
  const data = await response.json()
  return new Response(JSON.stringify(data), {
    headers: { 'Content-Type': 'application/json' }
  })
})
```

### 3. รับ OpenAI API Key

1. ไปที่ https://platform.openai.com/api-keys
2. สร้าง API key ใหม่
3. คัดลอก key และเพิ่มในไฟล์ `.env`

**หมายเหตุ:** API key จะมีค่าใช้จ่าย ตรวจสอบราคาที่ https://openai.com/pricing

## การใช้งาน

### ในหน้าจัดการประกาศ

1. **สร้างประกาศใหม่:**
   - กรอกหัวข้อและเนื้อหาประกาศ
   - คลิกปุ่ม "Generate HTML ด้วย AI" (ไอคอน Sparkles ✨)
   - รอสักครู่ AI จะสร้าง HTML ให้
   - คลิก "ดูตัวอย่าง" เพื่อเปิดในแท็บใหม่
   - คลิก "ดาวน์โหลด HTML" เพื่อบันทึกไฟล์

2. **Generate จากประกาศที่มีอยู่:**
   - คลิกปุ่มไอคอน Sparkles ✨ ในการ์ดประกาศ
   - ไฟล์ HTML จะถูกสร้างและดาวน์โหลดอัตโนมัติ

## ฟีเจอร์ของ HTML ที่ AI สร้าง

- 🎨 การออกแบบที่สวยงามและทันสมัย
- 🌈 Gradient สีสันสดใส
- ✨ Animation เบาๆ
- 📱 Responsive design (รองรับมือถือ)
- 🎬 ฝัง YouTube video (ถ้ามี)
- 🏷️ Badge แสดงความสำคัญและประเภท
- 📅 แสดงวันที่เริ่ม-สิ้นสุด
- 🖨️ รองรับการพิมพ์

## ข้อควรระวัง

1. **ค่าใช้จ่าย:** การเรียก OpenAI API มีค่าใช้จ่าย ประมาณ $0.0001-0.0003 ต่อครั้ง
2. **Rate Limit:** OpenAI มีข้อจำกัดจำนวนการเรียก API ต่อนาที
3. **API Key Security:** ไม่ควรเปิดเผย API key ในโค้ดหรือ commit ลง Git
4. **Fallback:** ถ้า API ล้มเหลว ระบบจะแสดง error message

## การแก้ไขปัญหา

### Error: "OpenAI API key not configured"
- ตรวจสอบว่าได้เพิ่ม `VITE_OPENAI_API_KEY` ในไฟล์ `.env` แล้ว
- Restart development server

### Error: "Failed to generate HTML"
- ตรวจสอบว่า API key ถูกต้อง
- ตรวจสอบว่ามี credit เหลืออยู่ใน OpenAI account
- ตรวจสอบ network connection

### HTML ที่ได้ไม่สวยหรือไม่ตรงต้องการ
- ลองสร้างใหม่อีกครั้ง (AI จะสร้างแบบต่างกันทุกครั้ง)
- แก้ไข prompt ในไฟล์ `src/services/openaiApi.ts`

## ไฟล์ที่เกี่ยวข้อง

- `src/services/openaiApi.ts` - Service สำหรับเรียก OpenAI API
- `src/pages/admin/AnnouncementManagement.tsx` - หน้าจัดการประกาศ
- `.env` - ไฟล์ environment variables

## Model ที่รองรับ (อัปเดต 2025)

ระบบรองรับ OpenAI models ล่าสุด:

### 🌟 รุ่นใหม่ล่าสุด (2025)

| Model | ความเร็ว | คุณภาพ | จุดเด่น | แนะนำสำหรับ |
|-------|----------|---------|---------|-------------|
| **🌟 GPT-5** | ⚡⚡ | ⭐⭐⭐⭐⭐⭐ | ฉลาดที่สุด, เข้าใจบริบทลึก | HTML ที่ซับซ้อนมาก, ต้องการความสมบูรณ์แบบ |
| **⚡ GPT-5 Mini** | ⚡⚡⚡ | ⭐⭐⭐⭐⭐ | รุ่นใหม่ล่าสุด, สมดุลดี | ใช้งานทั่วไป, คุ้มค่าสูง |
| **🧠 o1** | ⚡ | ⭐⭐⭐⭐⭐⭐ | Reasoning model, คิดลึก | HTML ที่ต้องการการวิเคราะห์ซับซ้อน |
| **💡 o1-mini** | ⚡⚡ | ⭐⭐⭐⭐⭐ | Reasoning เร็ว | HTML ที่ต้องการตรรกะ แต่ไม่ซับซ้อนมาก |

### รุ่นก่อนหน้า (ยังใช้ได้ดี)

| Model | ราคา (per 1M tokens) | ความเร็ว | คุณภาพ | แนะนำสำหรับ |
|-------|---------------------|----------|---------|-------------|
| **GPT-4o** | $2.50 | ⚡⚡ | ⭐⭐⭐⭐⭐ | HTML คุณภาพสูง, ราคาสมเหตุสมผล |
| **GPT-4o Mini** | $0.15 | ⚡⚡⚡ | ⭐⭐⭐⭐ | ใช้งานทั่วไป, ประหยัด (แนะนำ) |
| **GPT-4 Turbo** | $10.00 | ⚡⚡ | ⭐⭐⭐⭐ | สมดุลระหว่างคุณภาพและราคา |
| **GPT-3.5 Turbo** | $0.50 | ⚡⚡⚡ | ⭐⭐ | ประหยัดสุด, HTML เบื้องต้น |

### วิธีเลือก Model

1. ในฟอร์มสร้างประกาศ จะมี dropdown "AI Model สำหรับ Generate HTML"
2. เลือก model ที่ต้องการก่อนคลิก "Generate HTML ด้วย AI"
3. Model เริ่มต้นคือ **GPT-4o Mini** (แนะนำ)

### คำแนะนำการเลือก Model 2025

- **ต้องการล่าสุด + ดีที่สุด:** ใช้ GPT-5 หรือ GPT-5 Mini
- **ต้องการ Reasoning/วิเคราะห์:** ใช้ o1 หรือ o1-mini
- **ประกาศทั่วไป:** ใช้ GPT-4o Mini (เร็ว + ถูก)
- **ประกาศสำคัญ:** ใช้ GPT-5 หรือ GPT-4o
- **ทดสอบ/ฝึกใช้:** ใช้ GPT-3.5 Turbo (ประหยัด)

### ความแตกต่างของ GPT-5

- **ความเข้าใจบริบท:** ดีกว่า GPT-4 มาก
- **ความคิดสร้างสรรค์:** สร้าง HTML ที่สวยงามและเป็นเอกลักษณ์มากขึ้น
- **การจัดการภาษาไทย:** เข้าใจและใช้ภาษาไทยได้ดีขึ้น
- **Multimodal:** เข้าใจความสัมพันธ์ระหว่างข้อความ, รูปภาพ, วิดีโอได้ดีขึ้น

### ความแตกต่างของ o1 Series

- **Reasoning:** คิดวิเคราะห์ก่อนตอบ (Chain of Thought)
- **ความแม่นยำ:** สูงกว่า GPT-4 ในงานที่ต้องการตรรกะ
- **เวลา:** ช้ากว่า GPT-4 เพราะต้องคิดวิเคราะห์
- **เหมาะสำหรับ:** HTML ที่มีโครงสร้างซับซ้อน, ต้องการ accessibility, SEO
- **ข้อจำกัด:** ไม่รองรับ system message และ temperature parameter
- **Reasoning Tokens:** ใช้ tokens มากในการคิด ระบบจะจัดสรร 16,000 tokens เพื่อให้มี tokens เหลือสำหรับ output

## ข้อควรทราบเกี่ยวกับ Models ใหม่

### o1 และ o1-mini
- ❌ ไม่รองรับ `system message` (ใช้ user message เท่านั้น)
- ❌ ไม่รองรับ `temperature` parameter
- ✅ ใช้ `max_completion_tokens` แทน `max_tokens` (16,000 tokens)
- 🧠 ใช้ `reasoning_tokens` มากในการคิด
- 🎯 เหมาะสำหรับงานที่ต้องการ reasoning และความแม่นยำสูง

### GPT-5 และ GPT-5 Mini
- ✅ รองรับ `system message` ปกติ
- ⚠️ `temperature` รองรับเฉพาะค่า default (1) เท่านั้น - ไม่สามารถปรับได้
- ✅ ใช้ `max_completion_tokens` แทน `max_tokens` (16,000 tokens)
- 🧠 ใช้ `reasoning_tokens` ในการคิดวิเคราะห์
- 🚀 ประสิทธิภาพดีกว่า GPT-4 ทุกด้าน
- ⚠️ **หมายเหตุ:** GPT-5 ใช้ reasoning tokens มาก อาจใช้เวลานานกว่า GPT-4

### GPT-4 Series (4o, 4o-mini, 4-turbo)
- ✅ รองรับ `system message`
- ✅ รองรับ `temperature` แบบปรับค่าได้ (0-2)
- ✅ ใช้ `max_tokens`
- 💪 เสถียร, ทดสอบมาดีแล้ว

### GPT-3.5 Turbo
- ✅ รองรับ `system message`
- ✅ รองรับ `temperature` แบบปรับค่าได้ (0-2)
- ✅ ใช้ `max_tokens`
- 💰 ราคาถูกที่สุด

**ระบบจะจัดการความแตกต่างเหล่านี้อัตโนมัติ คุณแค่เลือก model ที่ต้องการได้เลย!**
